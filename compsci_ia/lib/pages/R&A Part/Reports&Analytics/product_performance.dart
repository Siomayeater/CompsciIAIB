import 'package:flutter/material.dart';

class ProductPerformance extends StatelessWidget {
  const ProductPerformance({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Product Performance'),
      ),
      body: const Center(
        child: Text('Welcome to Product Performance!'),
      ),
    );
  }
}
